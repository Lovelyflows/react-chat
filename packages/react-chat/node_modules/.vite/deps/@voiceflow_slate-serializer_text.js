import {
  Element,
  Node,
  isLinkElement,
  isVariableElement
} from "./chunk-73TOVTWY.js";
import "./chunk-HGBJHKYI.js";
import "./chunk-Y4266AMN.js";
import "./chunk-WGAPYIUP.js";

// ../../node_modules/@voiceflow/slate-serializer/build/esm/serializers/text.mjs
var nodeToString = (node, options) => {
  if (!Element.isElement(node))
    return Node.string(node);
  if (isLinkElement(node) && options.markdownLinks)
    return `[${serializeToText(node.children)}](${node.url})`;
  if (isVariableElement(node) && options.encodeVariables)
    return `{${node.name}}`;
  return nodesToString(node.children, options).join("");
};
var nodesToString = (nodes, options) => nodes.map((node) => nodeToString(node, options));
var serializeToText = (content, { encodeVariables = true, markdownLinks = false } = {}) => nodesToString(content, { encodeVariables, markdownLinks }).join("\n").trim();
export {
  serializeToText
};
//# sourceMappingURL=@voiceflow_slate-serializer_text.js.map
