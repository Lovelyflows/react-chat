import {
  z
} from "./chunk-XY3T52Y7.js";
import "./chunk-WGAPYIUP.js";

// ../../node_modules/@voiceflow/dtos-interact/build/esm/action/action-type.enum.js
var ActionType = {
  OPEN_URL: "open_url"
};

// ../../node_modules/@voiceflow/dtos-interact/build/esm/action/action.dto.js
var BaseActionDTO = z.object({
  type: z.string(),
  payload: z.unknown()
}).passthrough();
var OpenURLActionDTO = BaseActionDTO.extend({
  type: z.literal(ActionType.OPEN_URL),
  payload: z.object({
    url: z.string()
  }).passthrough()
}).passthrough();

// ../../node_modules/@voiceflow/dtos-interact/build/esm/enum/canvas-visibility-enum.js
var CanvasVisibility = {
  FULL: "full",
  HIDDEN: "hidden",
  CROPPED: "cropped"
};

// ../../node_modules/@voiceflow/dtos-interact/build/esm/enum/carousel-layout.enum.js
var CarouselLayout = {
  LIST: "List",
  CAROUSEL: "Carousel"
};

// ../../node_modules/@voiceflow/dtos-interact/build/esm/enum/visual-type.enum.js
var VisualType = {
  APL: "apl",
  IMAGE: "image"
};

// ../../node_modules/@voiceflow/dtos-interact/build/esm/request/payload.dto.js
var ActionRequestPayloadDTO = z.object({
  actions: z.array(BaseActionDTO).optional()
});
var LabelRequestPayloadDTO = z.object({
  label: z.string().optional()
});
var ActionAndLabelRequestPayloadDTO = ActionRequestPayloadDTO.merge(LabelRequestPayloadDTO);

// ../../node_modules/@voiceflow/dtos-interact/build/esm/request/request-type.enum.js
var RequestType = {
  TEXT: "text",
  ACTION: "action",
  INTENT: "intent",
  LAUNCH: "launch",
  NO_REPLY: "no-reply",
  EXIT_SCENARIO: "exit-scenario"
};

// ../../node_modules/@voiceflow/dtos-interact/build/esm/request/utils.dto.js
var BaseRequestDTO = z.object({
  type: z.string(),
  payload: z.any().optional(),
  // payload can be string or object
  diagramID: z.string().optional()
});

// ../../node_modules/@voiceflow/dtos-interact/build/esm/request/action-request.dto.js
var ActionRequestDTO = BaseRequestDTO.extend({
  type: z.literal(RequestType.ACTION),
  payload: ActionAndLabelRequestPayloadDTO.passthrough().optional()
}).passthrough();
var isActionRequest = (value) => ActionRequestDTO.safeParse(value).success;

// ../../node_modules/@voiceflow/dtos-interact/build/esm/request/general/general-request.dto.js
var GeneralRequestDTO = BaseRequestDTO.extend({
  type: z.string().refine((val) => !Object.values(RequestType).includes(val)),
  payload: ActionAndLabelRequestPayloadDTO.passthrough().optional()
}).passthrough();
var isGeneralRequest = (value) => GeneralRequestDTO.safeParse(value).success;

// ../../node_modules/@voiceflow/dtos-interact/build/esm/request/general/path-request.dto.js
var PathRequestDTO = GeneralRequestDTO.extend({
  type: z.string().refine((val) => val.startsWith("path-")),
  payload: ActionAndLabelRequestPayloadDTO.passthrough().required({ label: true })
}).passthrough();
var isPathRequest = (request) => PathRequestDTO.safeParse(request).success;

// ../../node_modules/@voiceflow/dtos-interact/build/esm/request/intent/intent-request.dto.js
var VerboseValueDTO = z.object({
  rawText: z.string(),
  canonicalText: z.string(),
  startIndex: z.number()
});
var IntentRequestEntityDTO = z.object({
  name: z.string(),
  value: z.string(),
  query: z.string().optional(),
  verboseValue: z.array(VerboseValueDTO).optional()
}).passthrough();
var IntentRequestPayloadDTO = ActionAndLabelRequestPayloadDTO.extend({
  data: z.record(z.any()).optional(),
  query: z.string().default(""),
  entities: z.array(IntentRequestEntityDTO).default([]),
  intent: z.object({ name: z.string() }).passthrough(),
  confidence: z.number().optional()
}).passthrough();
var IntentRequestDTO = BaseRequestDTO.extend({
  type: z.literal(RequestType.INTENT),
  payload: IntentRequestPayloadDTO
}).passthrough();
var isIntentRequest = (value) => {
  const parsedResult = IntentRequestDTO.safeParse(value);
  return parsedResult.success && // Need to explicitly check that optional properties with default values exist before
  // Zod transforms the input data. When Zod transforms data with a `.default()` tag
  // it inserts the default value before validating that the value matches the type.
  //
  // This is problematic in use-cases where we use Zod purely as a type-guard, in which
  // case, we discard the parsed object and instead use the original object which does
  // not have default values injected, thus, creating a misleading type-guard.
  !!value && typeof value === "object" && "payload" in value && !!value.payload && typeof value.payload === "object" && "entities" in value.payload && "query" in value.payload;
};

// ../../node_modules/@voiceflow/dtos-interact/build/esm/request/intent/alexa-intent-request.dto.js
var AlexaIntentRequestDTO = IntentRequestDTO.extend({
  payload: IntentRequestPayloadDTO.required({ data: true, entities: true })
}).passthrough().describe("Legacy intent request type that `general-runtime` expects Alexa to send");
var isAlexaIntentRequest = (request) => AlexaIntentRequestDTO.safeParse(request).success;

// ../../node_modules/@voiceflow/dtos-interact/build/esm/request/intent/general-intent-request.dto.js
var LegacyIntentRequestDTO = IntentRequestDTO.extend({
  payload: IntentRequestPayloadDTO.omit({ data: true }).required({ entities: true })
}).passthrough().describe("The legacy intent request type that `general-runtime` expects from its clients.");
var isLegacyIntentRequest = (request) => LegacyIntentRequestDTO.safeParse(request).success;

// ../../node_modules/@voiceflow/dtos-interact/build/esm/request/launch-request.dto.js
var LaunchRequestDTO = BaseRequestDTO.extend({
  type: z.literal(RequestType.LAUNCH),
  payload: z.object({
    persona: z.string().optional()
  }).passthrough().optional()
}).passthrough();
var isLaunchRequest = (value) => LaunchRequestDTO.safeParse(value).success;

// ../../node_modules/@voiceflow/dtos-interact/build/esm/request/no-reply-request.dto.js
var NoReplyRequestDTO = BaseRequestDTO.omit({ payload: true }).extend({
  type: z.literal(RequestType.NO_REPLY)
}).passthrough();
var isNoReplyRequest = (value) => NoReplyRequestDTO.safeParse(value).success;

// ../../node_modules/@voiceflow/dtos-interact/build/esm/request/text-request.dto.js
var TextRequestDTO = BaseRequestDTO.extend({
  type: z.literal(RequestType.TEXT),
  payload: z.string()
}).passthrough();
var isTextRequest = (value) => TextRequestDTO.safeParse(value).success;

// ../../node_modules/@voiceflow/dtos-interact/build/esm/request/any-request.dto.js
var AnyRequestDTO = z.union([
  ActionRequestDTO,
  AlexaIntentRequestDTO,
  LegacyIntentRequestDTO,
  GeneralRequestDTO,
  IntentRequestDTO,
  LaunchRequestDTO,
  PathRequestDTO,
  NoReplyRequestDTO,
  TextRequestDTO
]);
var isAnyRequest = (value) => AnyRequestDTO.safeParse(value).success;

// ../../node_modules/@voiceflow/dtos-interact/build/esm/request/exit-scenario-request.dto.js
var ExitScenarioRequestDTO = BaseRequestDTO.omit({ payload: true }).extend({
  type: z.literal(RequestType.EXIT_SCENARIO),
  payload: z.object({
    intentID: z.string()
  })
}).passthrough();
var isExitScenarioRequest = (value) => ExitScenarioRequestDTO.safeParse(value).success;

// ../../node_modules/@voiceflow/dtos-interact/build/esm/request/request-config.dto.js
var RequestConfigDTO = z.object({
  tts: z.boolean().optional(),
  stopAll: z.boolean().optional(),
  stripSSML: z.boolean().optional(),
  stopTypes: z.array(z.string()).optional(),
  selfDelegate: z.boolean().optional(),
  excludeTypes: z.array(z.string()).optional()
});

// ../../node_modules/@voiceflow/dtos-interact/build/esm/text/text.dto.js
var SlateTextValueDTO = z.any();

// ../../node_modules/@voiceflow/dtos-interact/build/esm/trace/trace-type.enum.js
var TraceType = {
  LOG: "log",
  END: "end",
  TEXT: "text",
  PATH: "path",
  FLOW: "flow",
  GOTO: "goto",
  SPEAK: "speak",
  BLOCK: "block",
  DEBUG: "debug",
  CHOICE: "choice",
  STREAM: "stream",
  VISUAL: "visual",
  CARD_V2: "cardV2",
  CAROUSEL: "carousel",
  NO_REPLY: "no-reply",
  ENTITY_FILLING: "entity-filling",
  CHANNEL_ACTION: "channel-action",
  KNOWLEDGE_BASE: "knowledgeBase"
};

// ../../node_modules/@voiceflow/dtos-interact/build/esm/trace/utils.dto.js
var TracePathDTO = z.object({
  label: z.string().optional(),
  event: BaseRequestDTO.optional()
});
var ButtonDTO = z.object({
  name: z.string(),
  request: BaseRequestDTO.optional()
});
var BaseTraceDTO = z.object({
  type: z.nativeEnum(TraceType),
  paths: z.array(TracePathDTO).optional(),
  defaultPath: z.number().optional(),
  time: z.number().optional()
});

// ../../node_modules/@voiceflow/dtos-interact/build/esm/trace/block.dto.js
var BlockTraceDTO = BaseTraceDTO.extend({
  type: z.literal(TraceType.BLOCK),
  payload: z.object({
    blockID: z.string()
  })
});

// ../../node_modules/@voiceflow/dtos-interact/build/esm/trace/card.dto.js
var CardTraceCardDTO = z.object({
  title: z.string(),
  buttons: z.array(ButtonDTO),
  imageUrl: z.string().nullable(),
  description: z.object({
    text: z.string(),
    // slate is optional, but text is required
    slate: SlateTextValueDTO.optional()
  })
}).partial();
var CardTraceDTO = BaseTraceDTO.extend({
  type: z.literal(TraceType.CARD_V2),
  payload: CardTraceCardDTO
});

// ../../node_modules/@voiceflow/dtos-interact/build/esm/trace/carousel.dto.js
var CarouselTraceDTO = BaseTraceDTO.extend({
  type: z.literal(TraceType.CAROUSEL),
  payload: z.object({
    cards: z.array(CardTraceCardDTO),
    layout: z.nativeEnum(CarouselLayout)
  })
});

// ../../node_modules/@voiceflow/dtos-interact/build/esm/trace/channel-action.dto.js
var ChannelActionTraceDTO = BaseTraceDTO.extend({
  type: z.literal(TraceType.CHANNEL_ACTION),
  payload: z.object({
    name: z.string(),
    payload: z.record(z.any())
  })
});

// ../../node_modules/@voiceflow/dtos-interact/build/esm/trace/choice.dto.js
var ChoiceTraceDTO = BaseTraceDTO.extend({
  type: z.literal(TraceType.CHOICE),
  payload: z.object({
    buttons: z.array(ButtonDTO)
  })
});

// ../../node_modules/@voiceflow/dtos-interact/build/esm/trace/debug.dto.js
var DebugTraceDTO = BaseTraceDTO.extend({
  type: z.literal(TraceType.DEBUG),
  payload: z.object({
    type: z.string().optional(),
    message: z.string()
  })
});

// ../../node_modules/@voiceflow/dtos-interact/build/esm/trace/entity-filling.dto.js
var EntityFillingTraceDTO = BaseTraceDTO.extend({
  type: z.literal(TraceType.ENTITY_FILLING),
  payload: z.object({
    entityToFill: z.string(),
    intent: IntentRequestDTO
  })
});

// ../../node_modules/@voiceflow/dtos-interact/build/esm/trace/exit.dto.js
var ExitTraceDTO = BaseTraceDTO.extend({
  type: z.literal(TraceType.END)
});

// ../../node_modules/@voiceflow/dtos-interact/build/esm/trace/flow.dto.js
var FlowTraceDTO = BaseTraceDTO.extend({
  type: z.literal(TraceType.FLOW),
  payload: z.object({
    name: z.string().optional(),
    diagramID: z.string()
  })
});

// ../../node_modules/@voiceflow/dtos-interact/build/esm/trace/goto.dto.js
var GoToTraceDTO = BaseTraceDTO.extend({
  type: z.literal(TraceType.GOTO),
  payload: z.object({
    request: BaseRequestDTO
  })
});

// ../../node_modules/@voiceflow/dtos-interact/build/esm/trace/log.dto.js
var LogTraceDTO = BaseTraceDTO.extend({
  type: z.literal(TraceType.LOG),
  // TODO: define this later
  payload: z.record(z.unknown())
});

// ../../node_modules/@voiceflow/dtos-interact/build/esm/trace/noreply.dto.js
var NoReplyTraceDTO = BaseTraceDTO.extend({
  type: z.literal(TraceType.NO_REPLY),
  payload: z.object({
    timeout: z.number()
  })
});

// ../../node_modules/@voiceflow/dtos-interact/build/esm/trace/speak/trace-speak-type.enum.js
var TraceSpeakType = {
  AUDIO: "audio",
  MESSAGE: "message"
};

// ../../node_modules/@voiceflow/dtos-interact/build/esm/trace/speak/speak.dto.js
var SpeakTraceDTO = BaseTraceDTO.extend({
  type: z.literal(TraceType.SPEAK),
  payload: z.object({
    src: z.string().nullable().optional(),
    type: z.nativeEnum(TraceSpeakType),
    voice: z.string().optional(),
    isPrompt: z.boolean().optional()
  })
});

// ../../node_modules/@voiceflow/dtos-interact/build/esm/trace/stream/trace-stream-action.enum.js
var TraceStreamAction = {
  LOOP: "LOOP",
  PLAY: "PLAY",
  PAUSE: "PAUSE",
  END: "END"
};

// ../../node_modules/@voiceflow/dtos-interact/build/esm/trace/stream/stream.dto.js
var StreamTraceDTO = BaseTraceDTO.extend({
  type: z.literal(TraceType.STREAM),
  payload: z.object({
    src: z.string(),
    token: z.string(),
    action: z.nativeEnum(TraceStreamAction),
    loop: z.boolean().optional(),
    title: z.string().optional(),
    iconImage: z.string().optional(),
    description: z.string().optional(),
    backgroundImage: z.string().optional()
  })
});

// ../../node_modules/@voiceflow/dtos-interact/build/esm/trace/text.dto.js
var TextTraceDTO = BaseTraceDTO.extend({
  type: z.literal(TraceType.TEXT),
  payload: z.object({
    slate: z.object({
      content: SlateTextValueDTO
    }),
    delay: z.number().optional()
  })
});

// ../../node_modules/@voiceflow/dtos-interact/build/esm/trace/visual.dto.js
var VisualTraceImageDTO = z.object({
  visualType: z.literal(VisualType.IMAGE).optional(),
  image: z.string().nullable(),
  device: z.string().nullable().optional(),
  options: z.object({ loop: z.boolean().optional() }).optional(),
  frameType: z.string().optional(),
  dimensions: z.object({ width: z.number(), height: z.number() }).nullable().optional(),
  canvasVisibility: z.nativeEnum(CanvasVisibility).optional()
});
var VisualTraceDTO = BaseTraceDTO.extend({
  type: z.literal(TraceType.VISUAL),
  /**
   * APL is deprecated, so we only support Image for now.
   */
  payload: VisualTraceImageDTO
});
export {
  ActionAndLabelRequestPayloadDTO,
  ActionRequestDTO,
  ActionRequestPayloadDTO,
  ActionType,
  AlexaIntentRequestDTO,
  AnyRequestDTO,
  BaseActionDTO,
  BaseRequestDTO,
  BaseTraceDTO,
  BlockTraceDTO,
  ButtonDTO,
  CanvasVisibility,
  CardTraceCardDTO,
  CardTraceDTO,
  CarouselLayout,
  CarouselTraceDTO,
  ChannelActionTraceDTO,
  ChoiceTraceDTO,
  DebugTraceDTO,
  EntityFillingTraceDTO,
  ExitScenarioRequestDTO,
  ExitTraceDTO,
  FlowTraceDTO,
  GeneralRequestDTO,
  GoToTraceDTO,
  IntentRequestDTO,
  IntentRequestEntityDTO,
  IntentRequestPayloadDTO,
  LabelRequestPayloadDTO,
  LaunchRequestDTO,
  LegacyIntentRequestDTO,
  LogTraceDTO,
  NoReplyRequestDTO,
  NoReplyTraceDTO,
  OpenURLActionDTO,
  PathRequestDTO,
  RequestConfigDTO,
  RequestType,
  SlateTextValueDTO,
  SpeakTraceDTO,
  StreamTraceDTO,
  TextRequestDTO,
  TextTraceDTO,
  TracePathDTO,
  TraceSpeakType,
  TraceStreamAction,
  TraceType,
  VerboseValueDTO,
  VisualTraceDTO,
  VisualTraceImageDTO,
  VisualType,
  isActionRequest,
  isAlexaIntentRequest,
  isAnyRequest,
  isExitScenarioRequest,
  isGeneralRequest,
  isIntentRequest,
  isLaunchRequest,
  isLegacyIntentRequest,
  isNoReplyRequest,
  isPathRequest,
  isTextRequest
};
//# sourceMappingURL=@voiceflow_dtos-interact.js.map
